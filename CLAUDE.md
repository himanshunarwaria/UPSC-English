# Project Context

## Design System
This project strictly follows the design system defined in `DESIGN.md` at the project root.
- Use only colors, typography, scales, and spacing values explicitly defined in DESIGN.md.
- Do not invent framework defaults or generate arbitrary hex codes.
- Match all screen layouts (e.g., the split-screen UPSC module or timed RBI module) to the structures specified.